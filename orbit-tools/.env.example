# Copy to orbit-tools/.env (gitignored) or set in CI secrets.
# Never commit real values. Never ship to mobile/web clients.
#
# STAGING only: ydlwukjzqtssbzirnefs
# Host: https://ydlwukjzqtssbzirnefs.supabase.co
# Do not point this file at production.

# Direct Postgres (Staging Dashboard → Settings → Database → URI)
# postgres role password. Never the anon or service_role key.
ORBIT_DATABASE_URL=postgresql://postgres.ydlwukjzqtssbzirnefs:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres

# Optional: CLI linked to staging (if ORBIT_DATABASE_URL is unset)
# Run once: supabase link --project-ref ydlwukjzqtssbzirnefs
# ORBIT_USE_SUPABASE_CLI=1
